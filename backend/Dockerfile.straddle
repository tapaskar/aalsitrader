FROM node:20-alpine

WORKDIR /app

# Copy the single bundled JS file (esbuild output)
COPY dist/fargate-runner.js ./fargate-runner.js

# No npm install needed — esbuild bundles all dependencies
# The only external is 'ws' which esbuild bundles into the output

CMD ["node", "fargate-runner.js"]
